[pytest]
pythonpath = .
markers =
    network: hits a live external service (FRED); skipped by default, run explicitly with -m network
    slow: real MCMC sampling across several replications; runs by default but takes tens of seconds
addopts = -m "not network"
